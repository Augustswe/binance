# Binance 测试网量化交易系统 — 优化建议报告

> 检查范围：`binance-quant/` 全量源码 + 运行日志（`logs/quant.log`）+ 学习器状态（`data/learner_state.json`）
> 检查时间：2026-08-20　运行时模式：`live`（测试网）

## 一、总体评价

项目完成度相当高：Donchian 长周期趋势跟踪 + 移动止损 + 交易所侧止损兜底 + 自动学习进化 + 多模式并行 + Web 仪表盘 + launchd 守护，架构分层清晰（`core / engine / strategies / web`），回测与调参工具齐全。代码可读性、中文注释、容错意识都不错。

主要风险不在"会不会崩"，而在 **配置与运行时不一致**、**止损单接口（日志已实锤报错）**、以及**默认启用了项目自己判定为负期望的策略**。下面按优先级列出。

---

## 二、关键问题与优化建议

### P0 — 必须处理（影响实盘正确性 / 资金安全）

#### 1. 止损单接口报错（日志实锤，今天仍在发生）
- **证据**：`logs/quant.log` 14:12–14:15 反复出现
  ```
  -4120 Order type not supported for this endpoint. Please use the Algo Order API endpoints instead.
  -1106 Parameter 'reduceonly' sent when not required.
  ```
- **位置**：`core/exchange.py:269 place_stop_market`；`engine/execution.py:84` 调用时传了 `fill_qty`（algoOrder 的 `closePosition=true` 不应带 quantity）。
- **现状**：磁盘上的 `exchange.py` 已改成 `/fapi/v1/algoOrder` + `closePosition=true`（看起来已修），但报错来自**当前运行中的进程**——说明跑的是旧进程或另有代码路径仍走旧端点。
- **建议**：
  1. 重启进程加载新代码（`deploy/manual.sh restart`）；
  2. 用测试网实盘验证止损单能真正挂上（看 `🎯 交易所止损单已挂` 日志、并在交易所后台确认 algoOrder 存在）；
  3. `place_stop_market` 去掉多余 quantity，返回值做成功校验并回写 `pos["stop_placed"]`，失败进事件流。
- **影响**：止损单挂不上 = 本地断网/行情断流时该持仓**失去交易所侧兜底**，只剩本地侧轮询止损（每 5s）——极端行情会扩大亏损。

#### 2. 默认启用 6 个模式，与项目自身风险结论矛盾
- **证据**：`config.yaml:14` `modes.enabled: [donchian, multi, grid, ma_cross, rsi, bollinger]`；但 `README.md` 明确写道："短周期（5m/15m/1h）策略已被回测证明负期望（手续费吃掉利润），已弃用"，且 grid/ma_cross/rsi/bollinger 是**为短周期设计的单策略引擎**（`strategies/engine.py:18 REGIME_WEIGHTS` 全是短周期参数）。
- **机制冲突**：多模式是"同币种竞争制"（一个币同一时间只容一个模式开仓，`trader.py:565-600`）。donchian 在列表最前、震荡市不出信号时**会让位**给 grid/rsi/ma_cross/bollinger 开仓——即把项目自己判为负期望的策略放到了生产仓位上。
- **建议**：默认只启 `donchian`（或 `donchian + multi`）。把 grid/ma_cross/rsi/bollinger 默认关掉，仅作为显式 A/B 测试时开启。
- **连带**：`config.yaml:9 strategy_mode: donchian` 字段已被 `modes.enabled` 架空（`strategies/modes.py:79` 优先读 `modes.enabled`），属于死配置，建议删除或加注释说明。

#### 3. 配置 `timeframe/donchian` 段被学习器覆盖，形同虚设
- **证据**：`trader.py:49-51` 启动时 `load_current_combo()` 立即覆盖 `self.timeframe`；`_maybe_learn` 每个周期用最优组合覆盖。当前 `learner_state.json` 显示实际运行组合 = `2h-20/10/2.0`，而 `config.yaml` 写的是 `timeframe: 1d` + `donchian 55/20/2.5`。
- **问题**：config 上写的周期/参数**完全不代表实际运行**，新人读 config 会被误导；且每次重启都重新从 `default_combo`/上次学习结果开始，不在 config 里。
- **建议**：README/注释明确"donchian 段仅在 `learner.enabled: false` 时生效"；或把学习器选出的基准组合也写回 config，避免配置漂移。

---

### P1 — 健壮性 / 安全（生产环境必做）

#### 4. 网络无重试退避，且行情失败会中断整个 tick
- **证据**：`core/exchange.py` 的 `requests.Session` 直连无重试；`logs/quant.log` 大量 `NameResolutionError` / `SSLEOFError`（代理/网络抖动）。`_tick`（`trader.py:297-303`）一旦 `get_all_prices` 失败就 `return`，**跳过**了后续 TP/SL 检查、移动止损、策略信号、学习调度。
- **风险**：虽然有交易所侧止损兜底，但本地 TP/SL、移动止损巡检、学习都被跳过，整轮"假死"。
- **建议**：
  - 加 `requests` 重试适配器（指数退避 + 对 429/5xx 重试）；
  - 行情失败改为"记录 + 继续"，不要整轮 return（TP/SL 用上一轮价格仍可跑）；
  - 代理节点不可用时给 Telegram 告警（目前只有事件流）。

#### 5. 状态保存并发隐患
- **证据**：`core/state.py` 中 `snapshot()`/`add_event()` 读写在 `self.lock` 内，但 `save()`（`state.py:89`）**不在锁内**直接 `json.dump(self.data)`；而 `_maybe_learn`/`_maybe_tune`（`trader.py`）用 `asyncio.to_thread` 调 `state.save()`，与主循环并发。`json.dump` 期间若 `add_event` 触发 `list.append` 可能 `RuntimeError`。
- **建议**：`save()` 也用 `self.lock`（或改成后台定时落盘 + 已有的 tmp+replace 原子写已很好，补锁即可）。

#### 6. 死代码 `_try_open`
- **证据**：`trader.py:602-653` 的 `TradingEngine._try_open` 从未被调用，已被 `_try_open_mode`（`trader.py:852`）取代。其中止损逻辑还残留 `place_stop_market(..., fill_qty)` 旧写法。
- **建议**：删除，避免维护歧义和误用。

#### 7. Web 仪表盘无鉴权（部署到远程时高危）
- **证据**：`web/app.py` 监听 `127.0.0.1:8090`，本地 OK；但 `/api/settings/api`（`update_api`）可改写你的 API Key，`/api/settings/symbols` 可改交易对。README 提到 launchd + 手动守护（可能跑在 VPS）。
- **建议**：明确"切勿把 8090 暴露到 `0.0.0.0`"；若需远程访问，加一个简单的 header token 鉴权或反向代理 + 密码。`update_api` 明文写 `.env`、表单明文 HTTP 传 secret（测试网可接受，但 README 必须标注"勿用于真实资金"）。

---

### P2 — 性能（每天学习任务 / 高频 tick）

#### 8. 行情拉取浪费带宽
- **证据**：`get_all_prices` / `get_mark_prices` / `get_all_24hr` 每 5s 拉**全市场所有交易对**（几百个），实际只用 5 个（`trader.py:298-316`）。
- **建议**：改用批量接口 `ticker/price?symbols=["BTCUSDT",...]`（和 `ticker/24hr?symbols=...`）。体积与耗时大幅下降。

#### 9. 学习器重复拉 K 线 + 重复算 ATR
- **证据**：`core/learner.py:151 learn()` 对每个组合(18) × 每个币(5) 都 `get_klines_range` 重新拉 = 最多 90 次请求 + 18 个新 `requests.Session`；`_donchian_backtest` 每根 K 线重算 ATR（`atr(highs[max(0,i-40):i],...)`）。
- **关键**：同一 `(symbol, timeframe)` 的 K 线对**所有组合共享**，应只拉一次并缓存。
- **建议**：学习前按 `(symbol, timeframe)` 预拉 K 线缓存 → 18 组合复用；可把每日学习任务从分钟级降到秒级，并避免触发行情限频。

#### 10. 学习器策略池硬编码
- **证据**：`core/learner.py:25 STRATEGY_POOL` 写死 18 个组合。
- **建议**：从 `config.yaml` 读取（与 `modes` 一致），方便调参不碰代码。
- **附加**：评分 `avg_ret - 0.3*avg_dd` 对"交易次数过少"的组合没惩罚，今天选出的 `2h-20/10/2.0` 偏灵敏、偏短，有近期过拟合嫌疑。建议加入"最小交易次数"门槛与样本外校验。

---

### P3 — 细节 / 正确性打磨

#### 11. AI 调参（`tuning`）周期与系统不匹配，且半吊子
- **证据**：`config.yaml:121 tuning` 默认 `timeframe: 5m / bars: 2000 / symbol: BTCUSDT`，但系统跑 `1d`/`multi`；`tuner.py` 优化 `position.tp_atr/sl_atr` 对 donchian 几乎无效（donchian 用自身 `sl_atr`）。
- **建议**：tuning 关闭时明确标注；若启用，绑定到当前 `timeframe`，且排除 donchian 不用的参数段。

#### 12. `get_income` 未分页，今日0点权益可能估算偏差
- **证据**：`trader.py:253 _estimate_today_start_equity` 调 `get_income(start_ms=...)` 没传 `end_ms`，默认 `limit=1000`。忙的一天流水可能 >1000 条被截断，导致 `day_start_equity` 不准。
- **建议**：分页拉全量（按 `startTime` 游标翻页）。

#### 13. 未收盘 K 线即判突破（设计权衡，提醒）
- **证据**：donchian `analyze`（`donchian.py:80`）用 `closes[-1]`（当前未收盘 K 线收盘价）判突破；学习器当前切到 `2h` 后，2h 内每 5 分钟都用实时价判突破。
- **影响**：可能在 K 线未收盘时"假突破"开仓，回撤又平。README 称此为"宁可早入"的有意设计，但配合 2h 周期会频繁触碰边界——建议实测 2h 下的开平仓频率，必要时改"仅在 K 线收盘后判信号"。

#### 14. 平仓用 `cancel_all_orders(symbol)` 全撤
- **证据**：`execution.py:104` 平仓前撤该币所有挂单。当前只有止损单，安全；但语义是"全撤"。
- **建议**：改为只撤该持仓相关的 algo 止损单（用 `algoId` 精确撤销），避免未来扩展其它挂单时被误撤。

---

## 三、优先行动清单（建议顺序）

1. **立刻**：重启进程加载新代码 → 测试网验证止损单能挂上（看日志 + 交易所后台），确认 `-4120/-1106` 不再出现。（对应 #1）
2. **今天**：`config.yaml` 把 `modes.enabled` 改为 `[donchian]`（或 `[donchian, multi]`），关掉 grid/ma_cross/rsi/bollinger；删除/注释失效的 `strategy_mode` 字段。（对应 #2/#3）
3. **本周**：给 `requests.Session` 加指数退避重试；行情失败改为"不中断 tick"；`save()` 加锁。（对应 #4/#5）
4. **性能**：学习器 K 线按 `(symbol,timeframe)` 缓存复用；行情改批量接口。（对应 #8/#9）
5. **清理**：删 `_try_open` 死代码；tuning 周期绑定；`get_income` 分页。（对应 #6/#11/#12）
6. **部署**：README 明确"8090 勿暴露公网 + 测试网勿接真实资金"。（对应 #7）

---

## 四、关键文件位置速查

| 关注点 | 文件 |
|---|---|
| 主循环 / 止损 / 学习调度 | `engine/trader.py` |
| 下单 / 平仓执行 | `engine/execution.py` |
| 交易所 REST 客户端（含止损单接口） | `core/exchange.py` |
| 自动学习进化器（策略池/评分） | `core/learner.py` |
| 风控（限额/熔断/冷静期） | `core/risk.py` |
| 状态持久化（并发风险点） | `core/state.py` |
| 配置加载 / 热更新 | `core/config.py` |
| 多模式管理 / 竞争逻辑 | `strategies/modes.py` |
| Donchian 策略 | `strategies/donchian.py` |
| Web 仪表盘 / 控制 API（无鉴权） | `web/app.py` |
| 全部配置（默认 6 模式风险源） | `config.yaml` |
